pipeline {
    agent any
    parameters {
        string(name: 'VERSION', defaultValue: 'Mr Jenkins', description: 'Who should I say hello to?')
   booleanParam(name: 'DEBUG_BUILD', defaultValue: true, description: '')
    }
 stages {
     stage('stage 1') {
         steps {
       echo 'Hello World' // some block
        }
     }
 }
}
